schema_version = 1
id = "modern-default-full-climate"
classification = "reusable-control"
purpose = "Modern default preset with climate parameters adjusted for full biome coverage (temperature/moisture extremes reachable)."
applicable_questions = [
  "biome reachability under a modern-default-like preset",
  "which biomes are structurally unreachable vs suppressed by narrow climate bands",
  "baseline biome distribution with UPLIFT continent type and archipelago",
]
known_limitations = [
  "climate.temperature and climate.moisture adjusted from user's exported preset to maximize climate coverage — not identical to any shipped or user preset",
  "worldHeight 640 / worldDepth 128 matches user's modern default, not RTF's compiled default",
]
minecraft_version = "1.21.1"
rtf_version = "0.0.6005"
base = "../_base"
overlay = "overlay"
